# Copy this file to `.env` next to docker-compose.yaml and customize.

# REQUIRED: shared auth token between pt-tools and cloakbrowser-manager.
# Generate with: openssl rand -hex 32
CLOAK_AUTH_TOKEN=your-secret-token-here

# Optional: set explicit timezone if needed
# TZ=Asia/Shanghai
